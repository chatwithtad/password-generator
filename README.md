# Password Generator

A Python password generator project with two versions:

- `random_password.py` uses Python's `random` module for learning and demonstration.
- `secure_password.py` uses Python's `secrets` module for security-sensitive password generation.

Both versions ask the user only for the desired password length and guarantee at least one letter, one number, and one symbol.